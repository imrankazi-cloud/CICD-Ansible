---
- name: Deploy MongoDB and Nginx
  hosts: all  # or your target group from /etc/ansible/hosts
  become: yes
  roles:
    - Ansible-Mongo-Role      # ← Full folder name from repo root
    - Nginx-Configuration    # ← Full folder name from repo root